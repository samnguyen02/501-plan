<% content_for :title, "Manager Dashboard - TAMU Ideathon 2026" %>

<% content_for :head do %>
<style>
  .glass-card {
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }
  .glass-card:hover {
    background: rgba(255, 255, 255, 0.08);
    border-color: rgba(255, 215, 0, 0.3);
  }
  .glass-search {
    background: rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    border: 1px solid rgba(255, 255, 255, 0.15);
  }
  .glass-search:focus {
    border-color: rgba(255, 215, 0, 0.5);
    box-shadow: 0 0 20px rgba(255, 215, 0, 0.15);
    outline: none;
  }
  .btn-delete {
    background: rgba(255, 49, 49, 0.15);
    border: 1px solid rgba(255, 49, 49, 0.3);
    transition: all 0.2s ease;
  }
  .btn-delete:hover {
    background: rgba(255, 49, 49, 0.3);
    border-color: rgba(255, 49, 49, 0.6);
    box-shadow: 0 0 15px rgba(255, 49, 49, 0.2);
  }
  .badge {
    background: rgba(255, 215, 0, 0.15);
    border: 1px solid rgba(255, 215, 0, 0.3);
  }
  .sort-btn {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.15);
    transition: all 0.2s ease;
  }
  .sort-btn:hover {
    background: rgba(255, 255, 255, 0.1);
  }
  .sort-btn-active {
    background: rgba(255, 215, 0, 0.15);
    border-color: rgba(255, 215, 0, 0.4);
    color: #FFD700;
  }
  .team-header {
    background: rgba(255, 215, 0, 0.08);
    border: 1px solid rgba(255, 215, 0, 0.2);
  }
  .attr-box {
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.08);
  }
</style>
<% end %>

<div class="min-h-screen px-4 py-8 sm:px-6 lg:px-8">
  <!-- Top bar -->
  <div class="max-w-5xl mx-auto mb-6 flex items-center justify-between">
    <%= link_to root_path, class: "inline-flex items-center gap-2 text-ideathon-yellow/70 hover:text-ideathon-yellow transition-colors text-sm" do %>
      <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7" />
      </svg>
      Back to Home
    <% end %>
    <div class="flex items-center gap-4">
      <%= link_to "Export Participants CSV",
                  export_participants_manager_index_path(request.query_parameters.merge(format: :csv)),
                  class: "text-xs px-3 py-1.5 rounded-md bg-white/10 hover:bg-white/20 text-white transition-colors" %>
      <%= link_to "Export Teams CSV",
                  export_teams_manager_index_path(request.query_parameters.merge(format: :csv)),
                  class: "text-xs px-3 py-1.5 rounded-md bg-white/10 hover:bg-white/20 text-white transition-colors" %>
      <%= link_to "Sign Out", destroy_admin_session_path, class: "text-white/30 hover:text-white/60 text-xs transition-colors" %>
    </div>
  </div>

  <div class="max-w-5xl mx-auto">
    <!-- Header -->
    <div class="mb-10">
      <h1 class="text-3xl sm:text-4xl font-urban-blocker text-ideathon-yellow tracking-wide">
        Manager Dashboard
      </h1>
      <p class="text-white/50 text-sm mt-1">
        Manage registered attendees
      </p>
      <div class="mt-6 flex gap-3">
        <span class="badge rounded-full px-4 py-1.5 text-ideathon-yellow text-sm font-semibold">
          <%= @registered_attendees.size %> attendee<%= @registered_attendees.size == 1 ? '' : 's' %>
        </span>
        <span class="badge rounded-full px-4 py-1.5 text-ideathon-yellow text-sm font-semibold">
          <%= @teams_count %> team<%= @teams_count == 1 ? '' : 's' %>
        </span>
      </div>
    </div>

    <!-- Search + Sort Controls -->
    <div class="flex flex-col sm:flex-row gap-4 mb-8">
      <!-- Search -->
      <%= form_with url: manager_index_path, method: :get, data: { controller: "manager-search", turbo_frame: "attendees_list" }, class: "flex-1" do |f| %>
        <%= f.hidden_field :sort, value: @sort %>
        <div class="relative">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 absolute left-4 top-1/2 -translate-y-1/2 text-white/30" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
          </svg>
          <%= f.text_field :query,
              value: params[:query],
              placeholder: "Search by name or team...",
              autocomplete: "off",
              class: "w-full glass-search rounded-xl pl-12 pr-4 py-3 text-white placeholder-white/30 text-sm",
              data: { manager_search_target: "input", action: "input->manager-search#search" } %>
        </div>
      <% end %>

      <!-- Sort Toggle -->
      <div class="flex gap-2 flex-shrink-0">
        <%= link_to manager_index_path(sort: "name", query: params[:query]),
            class: "sort-btn #{@sort == 'name' ? 'sort-btn-active' : 'text-white/50'} rounded-xl px-4 py-3 text-sm font-semibold inline-flex items-center gap-2",
            data: { turbo_frame: "attendees_list" } do %>
          <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
          </svg>
          By Name
        <% end %>
        <%= link_to manager_index_path(sort: "team", query: params[:query]),
            class: "sort-btn #{@sort == 'team' ? 'sort-btn-active' : 'text-white/50'} rounded-xl px-4 py-3 text-sm font-semibold inline-flex items-center gap-2",
            data: { turbo_frame: "attendees_list" } do %>
          <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
          </svg>
          By Team
        <% end %>
      </div>
    </div>

    <!-- Attendee List -->
    <%= turbo_frame_tag "attendees_list" do %>
      <% if @registered_attendees.any? %>
        <% if @sort == "team" %>
          <%# Group attendees by team name for team sort view %>
          <% grouped = @registered_attendees.group_by { |a| a.team&.unassigned ? "Unassigned" : (a.team&.team_name || "No Team") } %>
          <div class="space-y-6">
            <% grouped.each do |team_name, attendees| %>
              <div>
                <div class="team-header rounded-lg px-4 py-2 mb-3">
                  <h2 class="text-ideathon-yellow/80 text-sm font-semibold inline-flex items-center gap-2">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                      <path stroke-linecap="round" stroke-linejoin="round" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
                    </svg>
                    <%= team_name %>
                    <span class="text-white/30 font-normal">(<%= attendees.size %>)</span>
                  </h2>
                </div>
                <div class="space-y-3">
                  <% attendees.each do |attendee| %>
                    <%= render partial: "manager/attendee_card", locals: { attendee: attendee } %>
                  <% end %>
                </div>
              </div>
            <% end %>
          </div>
        <% else %>
          <div class="space-y-3">
            <% @registered_attendees.each do |attendee| %>
              <%= render partial: "manager/attendee_card", locals: { attendee: attendee } %>
            <% end %>
          </div>
        <% end %>
      <% else %>
        <div class="glass-card rounded-xl p-12 text-center">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 mx-auto text-white/20 mb-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1">
            <path stroke-linecap="round" stroke-linejoin="round" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
          </svg>
          <p class="text-white/40 text-sm">
            <% if params[:query].present? %>
              No attendees match "<%= params[:query] %>"
            <% else %>
              No registered attendees yet.
            <% end %>
          </p>
        </div>
      <% end %>
    <% end %>
  </div>

</div>
