<div id="ideathon_event_<%= event.id %>" class="glass-card rounded-xl p-4 sm:p-5 flex flex-col sm:flex-row sm:items-center gap-3 transition-all duration-200">
  <!-- Event info -->
  <div class="flex-1 min-w-0">
    <h3 class="text-white font-semibold text-base truncate">
      <%= event.event_name %>
    </h3>
    <div class="flex flex-wrap gap-2 mt-3">
      <span class="attr-box rounded-lg px-3 py-1.5 text-xs text-white/60 inline-flex items-center gap-1.5">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5 text-white/30" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
        </svg>
        <%= event.event_date.strftime("%B %d, %Y") %>
      </span>
      <span class="attr-box rounded-lg px-3 py-1.5 text-xs text-white/60 inline-flex items-center gap-1.5">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5 text-white/30" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <%= event.event_time.strftime("%I:%M %p") %>
      </span>
      <% if event.event_description.present? %>
        <span class="attr-box rounded-lg px-3 py-1.5 text-xs text-white/60 inline-flex items-center gap-1.5">
          <%= truncate(event.event_description, length: 60) %>
        </span>
      <% end %>
    </div>
  </div>

  <!-- Action buttons -->
  <div class="flex-shrink-0 flex items-stretch gap-2">
    <%= link_to edit_ideathon_event_path(event),
        class: "btn-edit rounded-lg px-3 py-2 accent-text inline-flex items-center justify-center cursor-pointer",
        data: { tooltip: "Edit event" } do %>
      <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
      </svg>
    <% end %>
    <div class="relative self-stretch flex items-stretch" data-tooltip="Delete event">
      <%= button_to ideathon_event_path(event), method: :delete,
          class: "btn-delete rounded-lg px-3 py-2 text-red-400 hover:text-red-300 cursor-pointer w-full",
          form_class: "flex h-full",
          data: { turbo_confirm: "Delete the event \"#{event.event_name}\"?" } do %>
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
        </svg>
      <% end %>
    </div>
  </div>
</div>
